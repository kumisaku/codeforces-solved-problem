#include <stdio.h>
#include <math.h>


int main(){
    int t;
    scanf("%d", &t);
    for (int z = 1; z <= t; z++){
        int n,k, sum = 0;
        scanf("%d %d", &n, &k);
        for (int i = 0; i < n; i++)
        {
            /* code */
        }
        
    }
    return 0;
